// book array
const myLibrary = [];

// the constructor...
function Book() {
    // should have the book’s id, title, author, the number of pages, and whether or not you have read the book.
}

// take params, create a book then store it in the array
function addBookToLibrary() {
}